# Copy this file to .env and fill in your own values.
# .env is in .gitignore on purpose - never commit real secrets or passwords.

PORT=3000
FRONTEND_URL=http://localhost:5173

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=roba
DB_NAME=pms_db

# Any long random string. You can generate one at the terminal with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=abdfshjdvfjsfshdbfajdvbh
JWT_EXPIRES_IN=1d
